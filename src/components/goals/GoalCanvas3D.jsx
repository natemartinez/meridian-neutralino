import React from 'react';

/**
 * GoalCanvas3D — previously rendered 3D goal planets via React Three Fiber.
 * Now replaced by the 2D canvas rendering in drawPages.js (drawGoalsPage).
 * This component is kept as a no-op placeholder to avoid breaking imports.
 */
export default function GoalCanvas3D() {
  return null;
}
